server:
  port: 6868
eureka:
  client:
    #eureka服务本身不需要注册到服务列表中
    register-with-eureka: false
    #eureka服务本身不需要获取其他的服务
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/